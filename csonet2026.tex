\documentclass[runningheads]{llncs}
%
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{graphicx}
\graphicspath{{images/}}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric}

\setlength{\textfloatsep}{4pt plus 2pt minus 2pt}
\setlength{\intextsep}{4pt plus 2pt minus 2pt}
\setlength{\floatsep}{4pt plus 2pt minus 2pt}
\setlength{\dbltextfloatsep}{4pt plus 2pt minus 2pt}
\setlength{\dblfloatsep}{4pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{2pt plus 1pt minus 1pt}
\setlength{\belowcaptionskip}{2pt plus 1pt minus 1pt}

\begin{document}
%
\title{Network-Aware Event Sequence Modeling for User-Behavior and System Log Anomaly Detection}
\titlerunning{Network-Aware Event Sequence Modeling for Log Anomaly Detection}

\author{Thanh Tien Cao\inst{1,2} \and
Tu Hoang Trinh\inst{2}\thanks{Corresponding authors: H. M. Tran (\email{hatm@huflit.edu.vn}) and T. H. Trinh (\email{tht.csec2005@gmail.com}, \email{23dh113972@st.huflit.edu.vn}). Source code, parsed template sequences, transition-graph artifacts, and per-seed metric tables are publicly available at: \protect\url{https://github.com/thtcsec/log-anomaly-tcn-transformer}. Institutional raw HTTP logs are \emph{not} released.} \and
Ha Manh Tran\inst{2}\textsuperscript{*}}
%
\authorrunning{T. T. Cao et al.}

\institute{Industrial University of Ho Chi Minh City, Ho Chi Minh City, Vietnam\\
\email{thanhct25471@pgr.iuh.edu.vn}
\and
Faculty of Information Technology, Ho Chi Minh City University of Foreign Languages - Information Technology, Ho Chi Minh City, Vietnam\\
\email{thanhct@huflit.edu.vn, tht.csec2005@gmail.com, hatm@huflit.edu.vn}}

\maketitle              % typeset the header of the contribution
%
\begin{abstract}
We present a network-aware log anomaly detection pipeline that treats Drain3 templates as vertices of a directed transition graph and each log sequence as a walk on that graph. Walks are scored with explicit transition-probability features (GraphWalk) in addition to PCA/SVD, Isolation Forest, a DeepLog-style LSTM, TCN, and a masked Transformer. On HDFS, frequency models remain competitive; on BGL, TCN and DeepLog-style LSTM achieve strong and nearly identical sequence performance; on HUFLIT-Career, compact-vocabulary outlier models remain competitive under heuristic/weak labels. An earlier VNICT~2026 report studied the HDFS/BGL backbone; this paper extends it with transition-graph scoring, HUFLIT-Career, leakage-aware splits, and deployment analyses.

\keywords{Log anomaly detection \and event-transition network \and Transformer \and Drain3 \and AIOps \and distributed systems.}
\end{abstract}
%
%
%
\section{Introduction}
Modern large-scale computing infrastructures, including distributed storage systems, supercomputers, web services, and cloud platforms, generate large volumes of logs with diverse formats and frequencies. These system logs capture both the operational state and the behavioral patterns of running services, while manually crafted detection rules quickly become obsolete as the infrastructure evolves. Effective log anomaly detection therefore requires analysis at the event sequence level rather than individual log lines. To capture these complex sequential relationships, we model the log stream as a directed event-transition network, where the vertices represent parsed event templates and the directed edges represent sequential transitions between events. A sequence of log messages generated by a specific operational unit (such as a Hadoop block or a client IP session) is thus represented as a walk (trajectory) on this directed transition network. In web-based environments like HUFLIT-Career, this state-transition network is coupled with a bipartite user-service interaction network where client IPs act as users executing walks across system routes. Detecting system anomalies is then formulated as identifying anomalous walks or trajectory deviations on these state-transition and user-interaction networks. Unsupervised models learn the normal traversal probabilities across network nodes, flagging walks that visit rare transitions or display quantitative anomalies.

This paper is positioned as an applied research and system evaluation study, rather than proposing a new anomaly detection algorithm. The central idea is to treat the event template sequence after Drain3 parsing as an intermediate representation layer: compact enough to run lightweight baselines, structured enough to extend to sequence models, and clear enough to map to operational units such as sessions, anonymized users, or time windows. Three research questions are addressed:
\begin{itemize}
    \item \textbf{RQ1:} How does the method of constructing event sequences after Drain3 affect the anomaly detection signal?
    \item \textbf{RQ2:} How do the scoring branches (reconstruction, isolation, graph-walk NLL, next-event prediction, causal convolution, and masked event modeling) compare on the same walks?
    \item \textbf{RQ3:} Does the performance ranking of these branches remain consistent when moving from public benchmark logs to an institutional web-log environment?
\end{itemize}

The main contributions of this work are four-fold: (1) a unified leakage-aware pipeline that structures raw logs as walks on directed event-transition (and, for web logs, client--service) graphs; (2) \emph{operational use} of that graph via Laplace-smoothed transition probabilities and GraphWalk / transition-feature scorers, alongside classical and deep branches; (3) a cross-dataset evaluation on HDFS, BGL, and HUFLIT-Career with an IP-disjoint ablation; (4) deployment trade-offs (F1, CPU/GPU latency, qualitative explainability) and an explicit discussion of proxy-label limits.

\paragraph{Relation to prior publication.}
A shorter preliminary version focused on the HDFS/BGL Drain3--TCN--Transformer comparison appeared at VNICT~2026~\cite{b26}. The present CSoNet paper extends that report with HUFLIT-Career, explicit transition-graph scoring, grouping/latency/explainability analyses, and a clearer statement of label and reproduction limitations. We do not claim the VNICT short paper and this manuscript are independent concurrent submissions of the same text.

To justify the academic and practical merits of this work, we outline its positioning across four key pillars:
\begin{itemize}
    \item \textbf{Necessary:} Modern systems generate gigabytes of log data per second. Traditional manual rules fail to capture contextual shifts. Unsupervised, sequence-level evaluation is necessary to automate anomaly detection in live systems.
    \item \textbf{Novelty:} This work provides a deployment-oriented, leakage-aware evaluation of frequency-based, isolation-based, \emph{graph-walk}, causal sequence, and masked-event modeling branches under a unified Drain3-based pipeline across public benchmarks and an institutional web-log dataset.
    \item \textbf{Significance:} Sequence models excel on complex BGL windows, while PCA and Isolation Forest remain competitive on structured or compact-vocabulary logs (HDFS, HUFLIT-Career). TCN achieves performance \emph{comparable} to the DeepLog-style LSTM under our protocol, with a modestly lower mean CPU latency (Table~\ref{tab_latency})---not a large accuracy or latency leap.
    \item \textbf{Applicability:} The pipeline is lightweight, uses Drain3 for online parsing, and operates on standard CPU/GPU resources, making it applicable to campus and enterprise monitoring after validation beyond weak labels.
\end{itemize}
Rather than proposing another detector, this work contributes a network-structured evaluation framework that identifies when frequency, isolation, graph-walk, and sequential scorers are preferable under different log-network regimes.

\section{Related Work}
The earliest log anomaly detection methods typically represented log data as event count vectors or feature matrices, and then applied traditional machine learning models such as PCA, clustering, or Isolation Forest \cite{b10,b11}. PCA is used to separate normal behavior space and detect points deviating from the principal subspace. This group of methods is lightweight and easy to deploy, but usually ignores the temporal order and contextual relationships between log events. Recently, comprehensive empirical studies (e.g., \cite{b24}) have re-evaluated the trade-offs between classical machine learning and deep learning, indicating that traditional models remain highly competitive on specific structures and resource-constrained environments.

To address this limitation, sequential models like DeepLog \cite{b1} use LSTM networks to predict the next event based on previous events. LogAnomaly \cite{b13} and LogRobust \cite{b14} extend this direction by combining sequential and semantic log information to deal with unstable log data. Additionally, semi-supervised approaches such as PLELog \cite{b15} use probabilistic label estimation to reduce dependence on manual labels. Overall, RNN/LSTM architectures capture sequence relationships better than traditional statistical methods, but still suffer from sequential processing limitations and are difficult to parallelize.

To represent unstructured logs for machine learning models, log parsing techniques are used to map each log line to a template. Drain \cite{b3} and Drain3 are widely used due to their fixed-depth parse tree structure, which allows high-speed online parsing. Recently, LogPPT \cite{b7} used prompt-based few-shot learning to improve parsing accuracy when labeled data is scarce.

The Transformer architecture with its self-attention mechanism \cite{b4} has been widely applied in AIOps. LogBERT \cite{b2} employs a Transformer encoder combined with masked event modeling to learn bidirectional representations for normal log sequences, improving contextual anomaly detection. Survey papers also note a strong shift from traditional sequential models to self-supervised deep learning based on Transformers \cite{b16}. In parallel, Large Language Model (LLM) and prompt learning approaches such as LogPrompt \cite{b8} and LogGPT \cite{b9} enhance semantic understanding, but face major barriers in terms of inference cost and real-time latency.

More recently, research has moved towards leveraging advanced foundation models, active domain adaptation, and parameter-efficient fine-tuning (PEFT). For instance, LogLLM \cite{b20} integrates BERT for semantic feature extraction and Llama for sequence classification using a projecting layer, demonstrating robustness on unstable logs without parser dependencies. Under the tested context, PEFT approaches like LINEADAPTER \cite{b23} and language model fine-tuning frameworks like LogFiT \cite{b22} have been proposed to adapt large language representations to log anomaly detection and root cause analysis with minimal training cost. To maintain consistency across different system deployments, cross-system domain adaptation techniques (e.g., LogAction \cite{b25}) have been developed to transfer learned anomaly boundaries to target environments. Additionally, to balance the trade-off between execution speed and semantic reasoning, collaborative frameworks like CoLA \cite{b21} combine lightweight Small Detection Models (SDMs) as anomaly filters with Large Language Models (LLMs) serving as high-fidelity expert classifiers and explainers.

\begin{table}[htbp]
\caption{Comparison of methodology groups and research gaps}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lp{2.2cm}p{3.2cm}p{3.2cm}}
\toprule
\textbf{Group} & \textbf{Representatives} & \textbf{Key Limitation} & \textbf{Improvement in This Work} \\
\midrule
Statistical & PCA, IF \cite{b10,b11,b24} & No temporal ordering & Lightweight baseline quantification \\
Sequential & DeepLog \cite{b1,b13} & Limited sequence length, sequential bottleneck & Parallel TCN integration \\
Transformer & LogBERT \cite{b2,b16} & Dependent on parsing quality & Decoupled parsing, grouping, and scoring \\
LLM / Foundation & LogGPT \cite{b9}, LogPrompt \cite{b8}, LogLLM \cite{b20}, CoLA \cite{b21}, LogFiT \cite{b22}, LINEADAPTER \cite{b23}, LogAction \cite{b25} & High inference latency and computational cost & We evaluate lightweight TCN and Transformer models for low-latency deployment \\
\bottomrule
\end{tabular}
\label{tab_related_work}
\end{center}
\end{table}

From existing works, four main research gaps can be identified: (i) traditional statistical methods do not learn the order of log events; (ii) sequential LSTM models are limited with long log sequences; (iii) Transformer models depend heavily on the parsing step; (iv) mapping anomaly detection pipelines to large networks requires clarifying the grouping strategy and business scenarios. 

To address these gaps, this paper builds a standardized pipeline that separates three phases (parsing, grouping, and scoring) using Drain3 as a preprocessing step to clean the token space. We evaluate in parallel bag-of-events baselines (PCA, TruncatedSVD, Isolation Forest), an explicit GraphWalk transition scorer, a DeepLog-style LSTM, TCN, and Transformer masked event modeling. Metrics are mean$\pm$std over 5 seeds. Source code, parsed sequences, and per-seed tables are public at \url{https://github.com/thtcsec/log-anomaly-tcn-transformer} (raw institutional HTTP logs are withheld).

\section{Proposed Methodology}
\subsection{Pipeline Overview}
The proposed pipeline is designed to clearly separate three processing layers: log standardization, event sequence construction, and anomaly detection. The input is a raw log dataset $L=\{l_1,l_2,\ldots,l_N\}$ generated from the operating system. The output is an anomaly label or score $a(S)$ for each event sequence $S$. In HDFS experiments, a sequence corresponds to a Block ID; in BGL experiments, a sequence corresponds to a fixed-size time window. In broader deployment scenarios, a sequence can correspond to a login session, an anonymized user, a service, or a sliding time window.

\begin{figure}[!t]
\centering
\resizebox{0.55\columnwidth}{!}{
\begin{tikzpicture}[
    node distance=0.35cm and 0.35cm,
    box/.style={rectangle, draw, rounded corners, align=center, minimum width=2.4cm, minimum height=0.5cm, font=\scriptsize},
    arrow/.style={-{Latex[length=1.5mm]}, thick}
]
\node[box] (raw) {Raw log streams\\(HDFS/BGL/HUFLIT)};
\node[box, below=of raw] (pre) {Preprocess\\\& Mask variables};
\node[box, below=of pre] (parse) {Drain3 parsing\\(Identify vertices $V$)};
\node[box, below=of parse] (seq) {Group events into Walks\\on transition/interaction graphs};
\node[box, below left=of seq] (vec) {Walk frequency view\\(BoE/TF-IDF)};
\node[box, below=of vec] (base) {Baselines\\(PCA/SVD/IF/GraphWalk)};
\node[box, below right=of seq] (bert) {Walk trajectory view\\(DeepLog/TCN/Transformer)};
\node[box, below=of bert] (seqscore) {Walk likelihood\\(NLL/top-$k$/trans.\ NLL)};
\node[box, below right=1.05cm and -1.45cm of base] (score) {Anomaly trajectory\\detection alert};
\draw[arrow] (raw) -- (pre);
\draw[arrow] (pre) -- (parse);
\draw[arrow] (parse) -- (seq);
\draw[arrow] (seq) -- (vec);
\draw[arrow] (vec) -- (base);
\draw[arrow] (seq) -- (bert);
\draw[arrow] (bert) -- (seqscore);
\draw[arrow] (base) -- (score);
\draw[arrow] (seqscore) -- (score);
\end{tikzpicture}
}
\caption{Proposed log anomaly detection pipeline block diagram}
\label{fig_pipeline}
\end{figure}

The overall process consists of six steps: (1) \textbf{Log Collection}: receive logs from source systems; (2) \textbf{Preprocessing}: normalize variable fields such as timestamps and identifiers; (3) \textbf{Log Parsing}: use Drain3 to map each log line to an event template and event ID; (4) \textbf{Event Grouping}: group event IDs by Block ID or by session/time window to create event sequences; (5) \textbf{Feature Representation}: generate bag-of-events/TF-IDF representations for baselines or keep raw event sequences for deep models; (6) \textbf{Anomaly Detection}: compute anomaly scores using reconstruction error, Isolation Forest, or sequence learning models.

\subsection{Preprocessing, Drain3 Parsing, and Event Grouping}
The input is a raw log set $L=\{l_1,l_2,\ldots,l_N\}$. The process starts by normalizing variable parameters (IP, timestamp, ID) and parsing raw logs into event templates using Drain3 \cite{b3}, which are then grouped into sequences.

Drain3 uses a fixed-depth tree to find the corresponding event template for each log line. The similarity between two log lines $T_1, T_2$ is calculated as the ratio of matching tokens:
\begin{equation}
Sim(T_1, T_2) = \frac{1}{n} \sum_{i=1}^{n} equ(t_{1,i}, t_{2,i})
\label{eq_sim}
\end{equation}
If $Sim(T_1, T_2) > st$ (similarity threshold, default 0.5), the log line is assigned to an existing template; otherwise, Drain3 creates a new template. After this step, each template is assigned an event ID, and the log sequence is represented as:
\begin{equation}
S = [E_1, E_2, ..., E_m]
\end{equation}
For HDFS, the sequence $S$ is grouped by Block ID. For BGL, it is grouped by non-overlapping windows of $W{=}100$ lines. We also support sliding window strategies to study the effect of the grouping strategy.

\subsection{Lightweight Machine Learning Baselines}
PCA and TruncatedSVD are applied to the bag-of-events vector representation $x = [count(E_j, S)]_{j=1}^d$ to learn the normal behavior subspace. The anomaly score is computed as the reconstruction error:
\begin{equation}
score(S)=\frac{1}{d}\sum_{j=1}^{d}(x_j-\hat{x}_j)^2
\label{eq_reconstruction}
\end{equation}
where $\hat{x}$ is the reconstructed vector after projecting onto the low-dimensional space (the number of principal components is set to 20 by default). The anomaly threshold $\tau$ is chosen as the 95th percentile on the normal training set. A sequence $S$ is labeled as anomalous if $score(S) > \tau$.
Isolation Forest is applied to TF-IDF bag-of-events vectors with $200$ trees. Unless noted, we set \texttt{contamination} to the training anomaly rate clipped to $[0.001,0.2]$ and also report a 95th-percentile threshold on $-\texttt{decision\_function}$ computed on normal training scores (same protocol family as PCA/NLL). Cross-dataset behaviour is highly non-uniform (Section~\ref{sec:if_variance}).

\subsection{Event-Transition Graph Scoring}
\label{sec:graphwalk}
To make the network framing operational---not only rhetorical---we build a directed first-order Markov graph $G=(V,E)$ from \emph{normal} training walks only. Vertices $V$ are Drain3 event IDs; a directed edge $u\!\rightarrow\!v$ counts observed successive co-occurrences. With Laplace smoothing $\alpha{=}1$ and vocabulary size $|V|$,
\begin{equation}
P(v\mid u)=\frac{c(u,v)+\alpha}{c(u)+\alpha|V|}.
\end{equation}
Each test walk $S=(E_1,\ldots,E_m)$ yields GraphWalk features: mean / worst-edge transition NLL, unseen-edge ratio, novel-node ratio, unique-event ratio, and length. We evaluate (i) GraphWalk mean-NLL with a 95th-percentile threshold on normal training walks, (ii) PCA / Isolation Forest on the same feature vectors, and (iii) a late-fusion ablation that averages $z$-scored GraphWalk NLL with $z$-scored TF-IDF Isolation Forest scores. This uses the transition graph as an explicit component of the anomaly score.

\subsection{Deep Sequence Models and Transformer}
\textbf{DeepLog-style LSTM:} We implement next-event prediction with a 2-layer LSTM inspired by DeepLog~\cite{b1}, \emph{not} a bit-exact reproduction of the original Spell parser, full HDFS corpus, or top-$g$ session rule. For each position $i$, the model predicts
\begin{equation}
\hat{P}(E_i \mid E_{i-g}, \ldots, E_{i-1}) = \text{softmax}(W_o h_i + b_o).
\end{equation}
In top-$k$ mode, a sequence is flagged if any actual event lies outside the top $K$ predictions. In NLL mode, the score is mean negative log-likelihood with a 95th-percentile threshold on normal validation scores. Our HDFS numbers use the first $500$k LogHub lines (full HDFS\_v1 has $>11$M lines), Drain3 templates, and the unified split above; they must \emph{not} be compared directly to $\sim$0.94--0.96 F1 figures reported elsewhere on full-data / different preprocessing protocols.

\textbf{Temporal Convolutional Network (TCN):} Integrated to improve parallelization and expand the receptive field. TCN utilizes dilated causal 1D convolutions \cite{b19}. The convolution operation on element $s$ of the input sequence $x$ with a filter $f$ of size $k$ and a dilation factor $d$ is defined as:
\begin{equation}
F(s) = (x *_d f)(s) = \sum_{j=0}^{k-1} f(j) \cdot x_{s - d \cdot j}
\end{equation}
A Chomp1d mechanism is used to crop the padding, ensuring causality. TCN uses the same dataset, scoring methods (top-k and NLL), and 95th percentile threshold selection as the DeepLog-style LSTM for a fair head-to-head under \emph{this} protocol.

\textbf{Transformer (LogBERT-inspired):} Employs a Transformer Encoder architecture combined with masked event modeling to learn bidirectional contexts \cite{b2}. A proportion of events (default 15\%) are replaced by a \texttt{[MASK]} token, and the model learns to recover them using cross-entropy loss:
\begin{equation}
L_{MLM} = -\sum_{i \in M} \log P(k_i \mid S_{masked})
\label{eq_loss}
\end{equation}
where $M$ is the set of masked positions. During inference, the anomaly score of sequence $S$ is the average MLM loss over the masked positions. An anomaly decision is made by comparing this score with a threshold $\tau$ selected as the 95th percentile on the normal validation set.

\section{Experimental Setup}
\subsection{Datasets}
The study utilizes HDFS and BGL from the LogHub benchmark \cite{b6,b12}, alongside HUFLIT-Career, a production-style institutional web-log dataset collected over a one-month period in June 2026 from a live university web application server. HDFS contains logs from a Hadoop system and is labeled by Block ID (anomalous if the block contains errors); we evaluate on the first 500,000 log lines. BGL contains logs from a supercomputer system, where anomaly labels are assigned using non-overlapping windows of $W{=}100$ lines. HUFLIT-Career consists of HTTP access logs from an online university career service. To protect user privacy, all sensitive client identification data, including client IP addresses and query payloads, were fully hashed and anonymized. Client IP addresses are utilized strictly for sequence grouping and are not included as features for any model. \textbf{Heuristic / weak labels:} scanner-probe labels were assigned by fixed export rules on \texttt{suspicious\_signals} (token \texttt{scanner\_probe}), not independently adjudicated SOC ground truth. Surface cues that trigger the rule can remain visible in method/path templates, so detectors may rediscover the labeling heuristic. We therefore interpret HUFLIT-Career metrics as \emph{agreement with the heuristic anomaly definition} rather than an unbiased estimate of real-world detection accuracy, report an IP-disjoint ablation for split leakage, and do not claim production SOC validity without gold labels. While raw HTTP files cannot be released, parsed template sequences, transition artifacts, and code are public. Dataset statistics after Drain3 appear in Table~\ref{tab_dataset_stats}.

\begin{table}[htbp]
\caption{Statistics of HDFS, BGL, and HUFLIT-Career datasets used in the experiments}
\begin{center}
\scriptsize
\begin{tabular}{lccc}
\toprule
\textbf{Metric} & \textbf{HDFS} & \textbf{BGL} & \textbf{HUFLIT-Career} \\
\midrule
Raw log lines & 500,000 & 500,000 & 119,516 \\
Grouping unit & Block ID & Window ($W{=}100$) & Client IP ($W{=}10$) \\
Number of sequences/windows & 36,305 & 5,000 & 25,049 \\
Number of normal sequences & 34,558 & 2,657 & 24,549 \\
Number of anomalous sequences & 1,747 & 2,343 (47\%) & 500 (2.0\%) \\
Number of event templates (Drain3) & 137 & 198 & 6 \\
Drain3 similarity threshold $\theta$ & 0.5 & 0.5 & 0.5 \\
Drain3 tree depth & 4 & 4 & 4 \\
\bottomrule
\end{tabular}
\label{tab_dataset_stats}
\end{center}
\end{table}

\textbf{Execution Environment:} The experiments were deployed on a computer configured with an Intel Core i7-13620H CPU, an RTX 4050 6GB GPU, 16GB RAM, running Ubuntu 22.04, Python 3.9, and PyTorch 2.0.

\subsection{Leakage-Aware Splitting and Threshold Selection}
To prevent data leakage, all unsupervised models (PCA, SVD, Isolation Forest, DeepLog, TCN, and Transformer) are trained strictly on normal sequences. For HDFS and BGL, the data split uses a 70/30 ratio for train/test sets (classical baselines) and a 60/20/20 ratio for train/validation/test sets (neural sequence models). The anomaly scoring threshold is computed as the 95th percentile of normal reconstruction errors or negative log-likelihood (NLL) on the validation set, ensuring that test labels are never accessed during threshold determination.

For the production-style HUFLIT-Career logs where sequence grouping is performed by client IP address, we adopt a sequence-level random split. Client IP addresses are used solely as keys for grouping log events into sequences and are completely excluded from model features. While some IP addresses might span both the training and testing sets through separate sequence windows (a limitation shared with sequence-level LogHub splits), using Event ID templates as the sole feature prevents direct leakage. To empirically validate the leakage-free generalization of our pipeline on HUFLIT-Career, we perform an additional ablation study using a strict IP-disjoint split (where 70\% of client IPs are used for training and the remaining 30\% are reserved exclusively for testing, ensuring zero client overlap). Under this zero-leakage protocol, the PCA baseline achieves a Precision of 0.5081, a Recall of 0.9127, and an F1-score of 0.6528. This confirms that the event transition patterns learned from normal traffic generalize robustly to entirely unseen client sessions.

Across all experiments, PCA and TruncatedSVD fix the number of principal components at 20. Isolation Forest uses 200 trees on TF-IDF with contamination $=$ clipped training anomaly rate (Section~3.3); we additionally analyze ranking via AUROC where noted. The mini Transformer uses 2 encoder layers ($d_{model}{=}64$, 4 heads, FF 128), AdamW~\cite{b17}, 5 epochs, mask ratio $r{=}0.15$. DeepLog-style LSTM and TCN use embedding/hidden size 64, Adam $10^{-3}$, 10 epochs. All headline tables report mean$\pm$std over seeds $\{21,42,84,123,777\}$ unless a caption states otherwise.

\textbf{Evaluation Metrics:} We use Precision, Recall, and F1-score to evaluate performance:
\begin{equation}
P = \frac{TP}{TP + FP}, \quad R = \frac{TP}{TP + FN}, \quad F1 = \frac{2 \cdot P \cdot R}{P + R}
\label{eq_metrics}
\end{equation}
In actual network operations, a low Precision causes many false alarms, while a low Recall leads to missed incidents. Therefore, F1-score is the primary metric for comparing performance.

\section{Results and Discussion}
\subsection{Cross-Dataset Performance Comparison}
The results in Table~\ref{tab_cross_dataset} show that no single model dominates. On HDFS, PCA leads ($0.5711 \pm 0.0944$); the DeepLog-style LSTM and TCN both score $0.4613 \pm 0.0199$ under our protocol (see Table~\ref{tab_per_seed_hdfs} for per-seed identity). The mini Transformer is weaker ($0.2950 \pm 0.0693$). HDFS anomalies are largely frequency shifts on short walks (mean length $13.77$, $|V|{=}137$), which favors PCA. Our DeepLog-style scores are \emph{not} full-corpus literature reproductions: we use a 500k-line prefix, Drain3, and unified top-$k$/NLL thresholding rather than Spell + top-$g$ on $>11$M lines~\cite{b1}.

\begin{table}[htbp]
\caption{Cross-dataset F1-score comparison (mean $\pm$ std over 5 seeds). Bold indicates best performance; underline indicates second best. Detailed Precision and Recall metrics are available in the public repository.}
\centering
\scriptsize
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{HDFS (F1)} & \textbf{BGL (F1)} & \textbf{HUFLIT-Career (F1)} \\
\midrule
PCA & $\mathbf{0.5711 \pm 0.0944}$ & $0.9217 \pm 0.0098$ & $\underline{0.6065 \pm 0.1426}$ \\
TruncatedSVD & $0.4540 \pm 0.1366$ & $0.9356 \pm 0.0117$ & $0.0659 \pm 0.1474$ \\
Isolation Forest & $0.1328 \pm 0.0103$ & $0.0404 \pm 0.0070$ & $\mathbf{0.6292 \pm 0.0235}$ \\
\midrule
Transformer (unsup.) & $0.2458 \pm 0.0639$ & $0.9242 \pm 0.0085$ & N/A \\
Transformer (val-tuned) & $0.2950 \pm 0.0693$ & $0.9388 \pm 0.0080$ & $0.3520 \pm 0.0304$ \\
DeepLog-style (top-k) & $\underline{0.4613 \pm 0.0199}$ & $\underline{0.9820 \pm 0.0042}$ & $0.0665 \pm 0.0401$ \\
DeepLog-style (NLL) & $0.3046 \pm 0.0198$ & $0.9381 \pm 0.0075$ & $0.5916 \pm 0.0096$ \\
\midrule
\textbf{TCN (top-k)} & $\underline{0.4613 \pm 0.0199}$ & $\mathbf{0.9826 \pm 0.0022}$ & $0.0451 \pm 0.0167$ \\
\textbf{TCN (NLL)} & $0.3042 \pm 0.0236$ & $0.9474 \pm 0.0035$ & $0.5916 \pm 0.0096$ \\
GraphWalk (NLL)$^\dagger$ & --- & --- & $0.4573 \pm 0.0179$ \\
\bottomrule
\end{tabular}
\label{tab_cross_dataset}
\end{table}
{\footnotesize $^\dagger$HUFLIT-Career only in this revision; GraphWalk AUROC $=0.896\pm0.023$ (5 seeds). Per-seed dumps: \texttt{logs/}, \texttt{results/}.}

\textbf{Identical top-$k$ decisions on HDFS.} Under our 500k-line / Drain3 / top-$k{=}9$ protocol, DeepLog-style LSTM and TCN produce \emph{identical} binary decisions on every seed, hence identical mean$\pm$std (Table~\ref{tab_per_seed_hdfs}). This is a short-sequence / small-vocabulary effect (mean length $13.77$, $|V|{=}137$), not evidence of a shared implementation bug: NLL-mode F1 already diverges slightly across seeds. Per-seed JSON dumps are in the public repository.

\begin{table}[htbp]
\caption{Per-seed HDFS top-$k$ F1 (200k-line DeepLog/TCN artifact; identical decisions each seed)}
\centering
\scriptsize
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & 21 & 42 & 84 & 123 & 777 \\
\midrule
DeepLog-style (top-k) & 0.4865 & 0.4444 & 0.4725 & 0.4382 & 0.4649 \\
TCN (top-k) & 0.4865 & 0.4444 & 0.4725 & 0.4382 & 0.4649 \\
\bottomrule
\end{tabular}
\label{tab_per_seed_hdfs}
\end{table}

\textbf{Visual Analysis and Threshold Sensitivity:} Baselines are analyzed via ROC/PR in Fig.~\ref{fig_curves}. PCA/SVD maintain better separability than Isolation Forest on imbalanced HDFS; seed-42 AUROC for PCA, TruncatedSVD, and Isolation Forest is $0.7722$, $0.7536$, and $0.7136$ (AP $0.5613$, $0.5676$, $0.1645$).

\begin{figure}[htbp]
\centering
\includegraphics[width=0.48\textwidth]{roc_curves.png}
\hfill
\includegraphics[width=0.48\textwidth]{pr_curves.png}
\caption{ROC (left) and Precision--Recall (right) on HDFS (seed 42). Prefer vector PDF exports in the camera-ready bundle; enlarge on screen for curve detail.}
\label{fig_curves}
\end{figure}

On BGL, TCN (top-k) achieves $0.9826 \pm 0.0022$, comparable to the DeepLog-style LSTM ($0.9820 \pm 0.0042$; $\Delta$F1 $=0.0006$). In NLL mode, TCN ($0.9474 \pm 0.0035$) is only marginally ahead of DeepLog-style ($0.9381 \pm 0.0075$). We therefore report TCN as achieving performance \emph{comparable} to DeepLog-style LSTM under the tested configuration---not clearly superior. PCA/SVD also remain strong ($0.9217$ / $0.9356$).

On HUFLIT-Career (heuristic labels), Isolation Forest leads F1 ($0.6292 \pm 0.0235$), then PCA ($0.6065 \pm 0.1426$). DeepLog-style and TCN NLL tie at $0.5916 \pm 0.0096$ per seed (same NLL decisions). GraphWalk mean-NLL yields $0.457\pm0.018$ F1 but strong ranking (AUROC $0.896\pm0.023$). Late fusion of $z$-scored GraphWalk NLL with $z$-scored TF-IDF IF scores reaches AUROC $0.899\pm0.023$ (Table~\ref{tab_graph_ablation}), showing the transition graph contributes measurable signal even when bag-of-events IF dominates F1 at a fixed percentile threshold.

\begin{table}[htbp]
\caption{HUFLIT-Career graph ablation (5 seeds): transition score vs.\ TF-IDF IF vs.\ late fusion}
\centering
\scriptsize
\begin{tabular}{lcc}
\toprule
\textbf{Scorer} & \textbf{F1 (95th pct.)} & \textbf{AUROC} \\
\midrule
GraphWalk NLL (transition only) & $0.457\pm0.018$ & $0.896\pm0.023$ \\
IF on TF-IDF (frequency) & $0.468\pm0.019^\ast$ & $0.898\pm0.023$ \\
Fusion $z$(GraphWalk)${+}z$(IF) & $0.468\pm0.018$ & $0.899\pm0.023$ \\
\bottomrule
\end{tabular}
\label{tab_graph_ablation}
\end{table}
{\footnotesize $^\ast$95th-percentile on $-\texttt{decision\_function}$; headline Table~\ref{tab_cross_dataset} IF uses contamination-based \texttt{predict}, hence higher F1.}

\subsection{Why Isolation Forest Varies Across Datasets}
\label{sec:if_variance}
Isolation Forest F1 swings from $0.13$ (HDFS) and $0.04$ (BGL) to $0.63$ (HUFLIT-Career under contamination decisions). (1)~\textbf{Outlier geometry:} Career anomalies are scanner-like in a $\sim$6-template space (TF-IDF density $\approx0.18$ on Career windows), so points isolate easily; HDFS/BGL anomalies often share frequent templates with normals. (2)~\textbf{Imbalance / contamination:} HDFS block anomaly rate $\sim$5\% with sparse high-dimensional TF-IDF yields low precision (AUROC still $0.71$ on HDFS seed 42, but AP only $0.16$). (3)~\textbf{BGL OR-window labels} mix normal and anomalous lines inside one bag, collapsing isolation cuts (F1$\approx0.04$) while sequence NLL/top-k still fire on local mismatches.

\subsection{Grouping Strategy Study (RQ1)}
To address RQ1, we evaluate BGL grouping with $W\in\{50,100,200\}$ (non-overlap) and sliding $W{=}100$, stride $50$. Table~\ref{tab_grouping} reports the controlled seed-42 reference on a 200k subset; the public script \texttt{scripts/bgl\_grouping\_experiment.py} repeats seeds $\{21,42,84,123,777\}$ for mean$\pm$std in the artifact (page limit keeps the compact reference table here).

Table~\ref{tab_grouping} shows PCA is sensitive to $W$ (better at $W{=}200$), while DeepLog-style/TCN stay robust; sliding windows give the best seed-42 F1 (DeepLog-style $0.9004$, TCN $0.9032$).

\begin{table}[htbp]
\caption{F1-score under different grouping configurations on BGL 200k (seed 42 reference; multi-seed artifact: \texttt{bgl\_grouping\_experiment.py})}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lccc}
\toprule
\textbf{Grouping Configuration} & \textbf{PCA} & \textbf{DeepLog (top-k)} & \textbf{TCN (top-k)} \\
\midrule
$W=50$ (non-overlap) & 0.1500 & 0.8556 & 0.8750 \\
$W=100$ (non-overlap) & 0.1353 & 0.8571 & 0.8769 \\
$W=200$ (non-overlap) & 0.4381 & 0.8810 & 0.8506 \\
$W=100$, stride $50$ (sliding) & 0.2314 & 0.9004 & 0.9032 \\
\bottomrule
\end{tabular}
\label{tab_grouping}
\end{center}
\end{table}

\subsection{Parameter Sensitivity, Latency, and Error Analysis}
\label{sec:ablation}
To understand the role of the three key hyperparameters, we perform three ablation studies on HDFS 200k (with seed 42): (i) Drain3 similarity threshold $\theta$, (ii) DeepLog top-k parameter $K$, and (iii) Transformer mask ratio $r$. Table~\ref{tab_ablation} and Table~\ref{tab_latency} summarize the results.

The ablation results provide three concrete insights. (A) The Drain3 similarity threshold has a strong impact on the pipeline quality: $\theta{=}0.3$ leads to over-merging (only 25 templates, resulting in low F1 due to mixing different events); $\theta{=}0.7$ causes template explosion (2,455 templates, causing F1 to collapse to $0.0446$); $\theta{=}0.5$ is the sweet spot, which is used as the default value throughout the paper. (B) DeepLog top-k saturates around $K{=}5$--$9$ with high precision and stable recall; $K{=}1$ is too strict (precision of 0.05), while $K{=}15$ begins to miss anomalies. (C) The mask ratio for the mini Transformer achieves the highest F1-score at the default value of $r{=}0.15$ — aligning with the classic BERT mask ratio \cite{b4}; increasing $r$ to 0.20--0.30 reduces effectiveness as too many tokens are masked. The vocabulary growth analysis shows that the number of templates increases from 13 (5k lines) to 137 (500k lines) in a logarithmic pattern, reflecting the template generation characteristics of real system logs.

\begin{table}[htbp]
\caption{Hyperparameter ablation study on HDFS 200k (seed 42)}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lcccc}
\toprule
\textbf{Configuration} & \textbf{Templates} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} \\
\midrule
\multicolumn{5}{l}{\textit{(A) Drain3 $\theta$, evaluated on PCA reconstruction}} \\
$\theta=0.3$ & 25 & 0.3695 & 0.5215 & 0.4325 \\
$\theta=0.5$ \textbf{(default)} & 68 & 0.4070 & 0.5550 & \textbf{0.4696} \\
$\theta=0.7$ & 2455 & 0.1000 & 0.0287 & 0.0446 \\
\midrule
\multicolumn{5}{l}{\textit{(B) DeepLog top-k}} \\
$K=1$ & 68 & 0.0466 & 0.6403 & 0.0869 \\
$K=3$ & 68 & 0.9091 & 0.2878 & 0.4372 \\
$K=5$ & 68 & 0.9524 & 0.2878 & 0.4420 \\
$K=9$ \textbf{(default)} & 68 & 0.9756 & 0.2878 & \textbf{0.4444} \\
$K=15$ & 68 & 0.9750 & 0.2806 & 0.4358 \\
\midrule
\multicolumn{5}{l}{\textit{(C) Transformer mask ratio $r$}} \\
$r=0.10$ & 68 & 0.1615 & 0.2230 & 0.1873 \\
$r=0.15$ \textbf{(default)} & 68 & 0.1968 & 0.2662 & \textbf{0.2263} \\
$r=0.20$ & 68 & 0.1711 & 0.2302 & 0.1963 \\
$r=0.30$ & 68 & 0.1701 & 0.2374 & 0.1982 \\
\bottomrule
\end{tabular}
\label{tab_ablation}
\end{center}
\end{table}

\textbf{Expected Error Analysis:} Four main types of errors are observed: (i) FP on rare sequences — normal but rare event sequences are incorrectly flagged as anomalies; (ii) FP on long sequences — reconstruction error naturally increases with sequence length, requiring normalization; (iii) FN when anomalies use common templates — requiring sequence models (TCN/DeepLog); (iv) template collision — Drain3 merges different logs due to a loose similarity threshold, requiring fine-tuning of $\theta$.

\textbf{Inference Latency and Deployment Cost:} A real-world log monitoring system must measure warning latency alongside the F1-score. To establish a benchmark, we measure the per-sample inference latency for each branch with a batch size of 1 on an RTX 4050 Laptop GPU (Drain3 and baselines measured on CPU; DeepLog/Transformer measured on both CPU and GPU) with 50 warm-up runs, averaged over 1,000 HDFS samples (Table~\ref{tab_latency}).

\begin{table}[htbp]
\caption{Per-sample inference latency, batch size 1, averaged over 1,000 HDFS samples (unit: ms)}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lcccc}
\toprule
\textbf{Branch} & \textbf{Mean} & \textbf{p95} & \textbf{p99} & \textbf{Throughput} \\
\midrule
Drain3 parse 1 line & 0.007 & 0.010 & 0.023 & 147,458 logs/s \\
PCA reconstruction & 0.063 & 0.153 & 0.219 & 15,869 sequences/s \\
TruncatedSVD & 0.176 & 0.281 & 0.381 & 5,689 sequences/s \\
Isolation Forest & 4.944 & 9.015 & 11.396 & 202 sequences/s \\
DeepLog (CPU) & 1.743 & 2.086 & 2.407 & 574 sequences/s \\
DeepLog (GPU) & 0.489 & 0.798 & 0.962 & 2,046 sequences/s \\
\textbf{TCN (CPU)} & 1.648 & 2.096 & 2.609 & 607 sequences/s \\
\textbf{TCN (GPU)} & 0.455 & 0.780 & 0.950 & 2,198 sequences/s \\
Transformer (CPU) & 2.395 & 2.966 & 3.267 & 418 sequences/s \\
Transformer (GPU) & 2.916 & 4.323 & 4.889 & 343 sequences/s \\
\bottomrule
\end{tabular}
\label{tab_latency}
\end{center}
\end{table}

Three observations from Table~\ref{tab_latency}: (1) Drain3 is not the bottleneck; (2) PCA/SVD are fastest; IF is much slower (200 trees); (3) TCN mean CPU latency ($1.648$~ms) is only modestly lower than DeepLog-style LSTM ($1.743$~ms, $\approx$5.5\%), while p95 is essentially tied ($2.096$ vs.\ $2.086$~ms)---we therefore do \emph{not} claim significantly lower latency without a formal significance test. Both benefit from GPU ($\sim$0.45~ms).

\subsection{Applications and Limitations}
The pipeline could potentially support detection of brute-force password scanning (repeated login failures), anomalous API utilization (new templates from Drain3), or service-overload patterns (HTTP 5xx sequences) in production networks after validation on corresponding production logs. This is a proposed application analysis that requires confirmation on anonymized production data.

\subsection{Qualitative Explainability Analysis}
To enhance operational utility, we present a qualitative explainability layer based on next-event prediction mismatches. Table~\ref{tab_explainability} illustrates two anomaly cases from HDFS and BGL using TCN. When flagged, the system outputs the failed event position, the actual versus top-3 predicted templates, and a severity score (NLL). For instance, in BGL sequence \texttt{W20200}, the unexpected occurrence of \texttt{E18} (data TLB error) instead of normal transitions (\texttt{E3}, \texttt{E6}, \texttt{E4}) produces a high score ($12.68$), directing operators to a virtual memory fault.

\begin{table}[htbp]
\caption{Qualitative explainability case studies for log anomalies}
\begin{center}
\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabular}{lccp{3.2cm}p{1.8cm}cp{2.2cm}}
\toprule
\textbf{Log Set} & \textbf{Seq ID} & \textbf{Pos} & \textbf{Mismatch Event (Actual)} & \textbf{Top-3 Predicted} & \textbf{NLL} & \textbf{Interpretation} \\
\midrule
HDFS & blk\_-68076... & 4 & E19: Receiving empty packet... & E5, E3, E4 & 12.39 & block write failure \\
BGL & W20200 & 1 & E18: data TLB error interrupt... & E3, E6, E4 & 12.68 & kernel page fault \\
\bottomrule
\end{tabular}
\label{tab_explainability}
\end{center}
\end{table}

\textbf{Threats to Validity:} (i) lab + campus logs still omit full SOC triage; (ii) domain shift; (iii)~\textbf{HUFLIT-Career labels are heuristic/weak}, so metrics measure agreement with the rule rather than unbiased detection accuracy; (iv) Drain3 $\theta$; (v) grouping and percentiles dominate F1; (vi) mini Transformer under-tuned; (vii) DeepLog-style/PCA numbers are protocol-matched reimplementations on a 500k-line prefix, not full-corpus literature reproductions; (viii) VNICT~2026~\cite{b26} overlaps the HDFS/BGL backbone---disclosed as a shorter prior report.

\section{Conclusion}
This paper evaluated a Drain3-based pipeline that \emph{uses} an event-transition graph (GraphWalk) alongside classical and deep scorers on HDFS, BGL, and HUFLIT-Career. No branch dominates: PCA remains competitive on HDFS; TCN and DeepLog-style LSTM are nearly tied on BGL; Isolation Forest leads under Career weak labels; GraphWalk adds measurable ranking signal. Choose scorers from vocabulary size, walk length, and label semantics. Future work: gold SOC labels, GNN/graph embeddings, and fuller HDFS protocols for literature-aligned baselines.

\vspace{-4pt}
\begin{thebibliography}{00}
\scriptsize
\setlength{\itemsep}{1pt plus 1pt}
\setlength{\parskip}{0pt}
\bibitem{b1} M. Du, F. Li, G. Zheng, and V. Srikumar, ``DeepLog: Anomaly Detection and Diagnosis from System Logs through Deep Learning,'' in \textit{Proc. ACM CCS}, 2017.
\bibitem{b2} H. Guo, S. Yuan, and X. Wu, ``LogBERT: Log Anomaly Detection via BERT,'' in \textit{Proc. IJCNN}, 2021.
\bibitem{b3} P. He, J. Zhu, Z. Zheng, and M. R. Lyu, ``Drain: An Online Log Parsing Approach with Fixed Depth Tree,'' in \textit{Proc. IEEE ICWS}, 2017.
\bibitem{b4} A. Vaswani et al., ``Attention Is All You Need,'' in \textit{Proc. NeurIPS}, 2017.
\bibitem{b5} S. He, J. Zhu, P. He, and M. R. Lyu, ``Experience Report: System Log Analysis for Anomaly Detection,'' in \textit{Proc. IEEE ISSRE}, 2016.
\bibitem{b6} J. Zhu et al., ``Tools and Benchmarks for Automated Log Parsing,'' in \textit{Proc. ICSE}, 2019.
\bibitem{b7} V. H. Le and H. Zhang, ``Log Parsing with Prompt-based Few-shot Learning,'' in \textit{Proc. ICSE}, 2023.
\bibitem{b8} T. Zhang, X. Huang, W. Zhao, S. Bian, and P. Du, ``LogPrompt: A Log-based Anomaly Detection Framework Using Prompts,'' in \textit{Proc. IJCNN}, 2023.
\bibitem{b9} X. Han, S. Yuan, and M. Trabelsi, ``LogGPT: Log Anomaly Detection via GPT,'' in \textit{Proc. IEEE BigData}, 2023.
\bibitem{b10} V. Chandola, A. Banerjee, and V. Kumar, ``Anomaly Detection: A Survey,'' \textit{ACM Comput. Surv.}, vol. 41, no. 3, 2009.
\bibitem{b11} F. T. Liu, K. M. Ting, and Z.-H. Zhou, ``Isolation Forest,'' in \textit{Proc. IEEE ICDM}, 2008.
\bibitem{b12} J. Zhu, S. He, J. Liu, M. R. Lyu, and P. He, ``Loghub: A Large Collection of System Log Datasets for AI-driven Log Analytics,'' in \textit{Proc. IEEE ISSRE}, pp. 355--366, 2023.
\bibitem{b13} W. Meng et al., ``LogAnomaly: Unsupervised Detection of Sequential and Quantitative Anomalies in Unstructured Logs,'' in \textit{Proc. IJCAI}, pp. 4739--4745, 2019.
\bibitem{b14} X. Zhang et al., ``Robust Log-Based Anomaly Detection on Unstable Log Data,'' in \textit{Proc. ESEC/FSE}, 2019.
\bibitem{b15} L. Yang et al., ``Semi-Supervised Log-Based Anomaly Detection via Probabilistic Label Estimation,'' in \textit{Proc. ICSE}, 2021.
\bibitem{b16} M. Landauer, S. Onder, F. Skopik, and M. Wurzenberger, ``Deep Learning for Anomaly Detection in Log Data: A Survey,'' \textit{Mach. Learn. Appl.}, vol. 12, 2023.
\bibitem{b17} I. Loshchilov and F. Hutter, ``Decoupled Weight Decay Regularization,'' in \textit{Proc. ICLR}, 2019.
\bibitem{b18} A. Oliner and J. Stearley, ``What Supercomputers Say: A Study of Five System Logs,'' in \textit{Proc. IEEE/IFIP DSN}, 2007.
\bibitem{b19} S. Bai, J. Z. Kolter, and V. Koltun, ``An Empirical Evaluation of Generic Convolutional and Recurrent Networks for Sequence Modeling,'' \textit{arXiv:1803.01271}, 2018.
\bibitem{b20} W. Guan, J. Cao, S. Qian, J. Gao, and C. Ouyang, ``LogLLM: Log-Based Anomaly Detection Using Large Language Models,'' \textit{arXiv preprint arXiv:2411.08561}, 2024. \url{https://doi.org/10.48550/arXiv.2411.08561}
\bibitem{b21} X. Zhu et al., ``CoLA: Model Collaboration for Log-based Anomaly Detection,'' \textit{Proc. VLDB Endow.}, vol. 18, no. 11, pp. 3979--3987, 2025. \url{https://doi.org/10.14778/3749646.3749668}
\bibitem{b22} C. Almodovar, F. Sabrina, S. Karimi, and S. Azad, ``LogFiT: Log Anomaly Detection Using Fine-Tuned Language Models,'' \textit{IEEE Trans. Netw. Service Manag.}, vol. 21, no. 2, pp. 1715--1723, 2024. \url{https://doi.org/10.1109/TNSM.2024.3358730}
\bibitem{b23} X. Liu, W. Bao, Y. Zhang, Y. Li, R. Ranjan, and D. N. Jha, ``LINEADAPTER: Parameter-Efficient Fine-Tuning for Log Anomaly Detection and Root Cause Analysis,'' \textit{preprint}, 2025.
\bibitem{b24} B. Yu et al., ``Deep Learning or Classical Machine Learning? An Empirical Study on Log-Based Anomaly Detection,'' in \textit{Proc. IEEE/ACM ICSE}, pp. 1--13, 2024. \url{https://doi.org/10.1145/3597503.3623308}
\bibitem{b25} C. Duan et al., ``LogAction: Consistent Cross-system Anomaly Detection through Logs via Active Domain Adaptation,'' \textit{arXiv preprint arXiv:2510.03288}, 2025.
\bibitem{b26} T. T. Cao, T. H. Trinh, and H. M. Tran, ``Application of TCN and Transformer Networks for Log Anomaly Detection in Large-Scale Enterprise and Industrial Networks,'' in \textit{Proc. VNICT}, 2026, EasyChair ID~\#6979. (Shorter preliminary version of the HDFS/BGL evaluation extended in this paper.)
\end{thebibliography}

\end{document}
